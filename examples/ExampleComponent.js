// package imports at the top
import React from 'react';

//internal component/helper function imports after space

const ExampleComponent = ({ deconstructPropsHere }) => {
  const logic = 'should be put above the return';
  return <div>This is where any render options need to go</div>;
};

//export default ExampleComponent
